-- Output generated code in $buildDir/generated/{ast,parser,scanner}
++ Clean generated source dirs before regenerating sources (Fixes problems when removing an AST class).
++ Dependency on inline module specification.
++ Rename configureModuleBuild() to configureModularBuild().
-- Generate warning if include rule does not match any files.


Check the cause of the following warning from publishing:

	> Task :publishPluginMavenPublicationToMavenRepository
	Multiple publications with coordinates 'org.jastadd:jastaddgradle:1.14.1' are published to repository 'maven'. The publications will overwrite each other!
